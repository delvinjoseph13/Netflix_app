import 'package:flutter/material.dart';

class ScreenNews extends StatelessWidget {
  const ScreenNews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('new&hot'),);
  }
}